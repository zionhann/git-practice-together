# CLAUDE.md

이 저장소에서 작업하는 모든 팀원(사람 · 에이전트)이 따르는 규칙입니다.

## 이 저장소

깃 · 깃허브 특강 팀 프로젝트용 예제 저장소입니다.
팀원이 각자 추천하는 유튜브 영상을 `index.html` 한 파일에 카드로 모읍니다.

- `index.html` — 카드 목록. **모든 작업은 여기서 일어납니다.**
- `style.css` — 공통 스타일. **고치지 않습니다.**

여러 명이 같은 파일의 같은 자리를 고치므로 **충돌은 정상입니다.**
충돌이 나면 아래 [충돌 해결](#충돌-해결) 규칙을 따릅니다.

## 개발 흐름

순서를 지킵니다. 한 단계라도 건너뛰지 않습니다.

1. **이슈 생성** — `🎬 클립 추가` 템플릿으로 이슈를 만들고 **이슈 번호를 확인**합니다
2. **브랜치 생성** — `main`에서 최신 상태를 받은 뒤 이슈 번호로 브랜치를 만듭니다
3. **작업 · 커밋** — 변경사항을 아래 커밋 컨벤션에 맞춰 커밋합니다
4. **푸시** — 원격에 브랜치를 올립니다
5. **PR 생성** — `main`을 대상으로 풀 리퀘스트를 만듭니다

```bash
git switch main
git pull

git switch -c 'feat/#12'          # 따옴표 필수 (아래 참고)

# index.html 에 카드 추가
git add index.html
git commit -m "feat: 관련 없는 두 사람의 연결고리 클립 추가"

git push -u origin 'feat/#12'
gh pr create --base main          # 또는 깃허브 웹에서 생성
```

## 브랜치 네이밍

```
feat/#{이슈번호}
```

예: 이슈 12번 → `feat/#12`

- 이슈 번호는 **반드시 실제로 생성된 이슈의 번호**입니다. 추측하지 않습니다.
- `#`는 셸에서 주석 시작으로 해석됩니다. 브랜치명은 항상 **작은따옴표로 감쌉니다**:
  `git switch -c 'feat/#12'`
- 브랜치는 이슈 하나당 하나입니다. 여러 이슈를 한 브랜치에 묶지 않습니다.

## 커밋 컨벤션

[Conventional Commits](https://www.conventionalcommits.org/ko/) 를 따릅니다.
자세한 규칙은 `conventional-commits` 스킬을 참고합니다.

```
<type>(<scope>): <description>

<body>

<footer>
```

**이 저장소의 관습**: 타입과 스코프는 영어, 설명은 한국어로 씁니다.

| 타입 | 언제 쓰나 | 이 저장소에서의 예 |
| --- | --- | --- |
| `feat` | 새 기능 / 콘텐츠 추가 | 클립 카드 추가 |
| `fix` | 잘못된 것 수정 | 깨진 썸네일 주소, 오타난 영상 ID |
| `docs` | 문서만 변경 | README 수정 |
| `style` | 서식 · 들여쓰기 (동작 변화 없음) | HTML 정렬 |
| `refactor` | 동작 그대로 구조만 변경 | 카드 순서 재배치 |
| `chore` | 그 외 설정 · 도구 | 이슈 템플릿 수정 |

규칙:

- 설명은 명령형으로, 마침표 없이: `클립 추가` (O) / `클립을 추가했습니다.` (X)
- 한 커밋에 한 가지 일만 담습니다
- 스코프는 선택입니다: `feat(index): ...`, `chore(github): ...`

예시:

```
feat: Never Gonna Give You Up 클립 추가
fix: 썸네일 주소의 영상 ID 오타 수정
docs: README 에 브랜치 규칙 추가
```

## PR 규칙

- 대상 브랜치는 항상 `main` 입니다
- `.github/PULL_REQUEST_TEMPLATE.md` 를 채웁니다
- 본문의 `Closes #` 뒤에 **이슈 번호를 반드시 적습니다**. 그래야 머지될 때 이슈가 자동으로 닫힙니다
- 리뷰가 승인되기 전에 직접 머지하지 않습니다

## 충돌 해결

**먼저 머지된 쪽이 우선입니다. 뒤에 오는 PR 이 직접 해결합니다.**

- 이미 `main` 에 들어간 카드는 건드리지 않습니다. 지우거나 자리를 옮기지 않습니다
- 내 PR 에 충돌이 뜨면, **내 브랜치에서** `main` 을 병합해 해결한 뒤 다시 푸시합니다
- `main` 브랜치 자체에서 충돌을 해결하지 않습니다
- 이 저장소에서는 `merge` 만 씁니다. `rebase` 는 쓰지 않습니다 (충돌 마커의 방향이 반대라 헷갈립니다)

```bash
git switch 'feat/#12'       # 내 브랜치
git fetch origin
git merge origin/main       # 여기서 충돌 발생

# index.html 을 열어 충돌 마커를 정리
git add index.html
git commit                  # 병합 커밋 (메시지는 기본값 그대로 두어도 됩니다)
git push
```

충돌 마커를 정리하는 방법:

```html
<<<<<<< HEAD
  ... 내 카드 ...          ← 내 브랜치의 내용
=======
  ... 먼저 머지된 카드 ...  ← main 의 내용 (이미 머지된 쪽)
>>>>>>> origin/main
```

- **`main` 쪽 카드를 위에, 내 카드를 아래에** 남기고 마커 세 줄(`<<<<<<<`, `=======`, `>>>>>>>`)을 지웁니다
- 어느 쪽도 지우지 않습니다. 둘 다 남아야 합니다
- 정리한 뒤 `index.html` 을 브라우저로 열어 카드가 모두 보이는지 확인합니다

## 에이전트가 특히 지킬 것

- **`main` 에 직접 커밋하지 않습니다.** 작업 전 현재 브랜치를 `git branch --show-current` 로 확인합니다
- 이슈 번호를 모르면 **만들어내지 말고 사용자에게 묻거나 `gh issue list` 로 확인**합니다
- `style.css` 는 사용자가 명시적으로 요청하지 않는 한 수정하지 않습니다
- 커밋 · 푸시 · PR 생성은 사용자가 요청했을 때만 실행합니다
- 카드를 추가할 때는 `index.html` 의 `▼▼▼ 카드 하나 시작` ~ `▲▲▲ 카드 하나 끝` 블록을 복사해 쓰고,
  영상 ID 두 군데(링크 · 썸네일)를 모두 바꿨는지 확인합니다
